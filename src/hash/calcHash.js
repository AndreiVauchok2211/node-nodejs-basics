const calculateHash = () => {
  // Write your code here
  console.log(
    "I read late that it was necessary to make a branch from the main one and carry out development in it"
  );
};

calculateHash();
